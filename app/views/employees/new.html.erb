<p>Employee Details</p>
    <%= form_for @employee do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
    <%= f.label :title %>
	<%= f.text_field :title %>
    <%= f.collection_select:company_id, Company.all, :id, :name %>
    <%= f.submit %>

    <% end %>